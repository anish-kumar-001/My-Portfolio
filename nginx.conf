server {
    listen 80;
    server_name localhost;
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # CRITICAL for React routing: serves index.html for all routes not found
        try_files $uri $uri/ /index.html;
    }
}